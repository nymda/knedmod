#include "mem.h"
#include "Global.h"

namespace rdvl {
	//too lazy to do this with scanning, its only one function and its easy to find
	void enableRDVL() {
		//mem::Nop((byte*)(glb::moduleBase + 0x1214f5), 8);
	}
}